import { intentProps } from "./intentProps";

export const intentsProps = `
  ${intentProps}
  children {
    ${intentProps}
    children {
      ${intentProps}
      children {
        ${intentProps}
        children {
          ${intentProps}
        }
      }
    }
  }
`;
